stages:
  - test
  - build_static_assets

.js:
  image: node
  before_script:
    - git --version
    - node --version
    - npm --version
    - yarn --version
    - yarn

test:
  only:
    refs:
      - master
  image: buildkite/puppeteer
  stage: test
  before_script:
    - apt-get update && apt-get install -y git
    - git --version
    - node --version
    - npm --version
    - yarn --version
    - yarn
  script:
    - yarn test

build_static_assets:
  extends: .js
  stage: build_static_assets
  script:
    - yarn build:prod
  artifacts:
    paths:
      - build/
